version: '3.8'

services:
  hicore:
    build: .
    container_name: unsiao-hicore
    ports:
      - "8000:8000"
    volumes:
      # 持久化数据库和工作目录
      - ./database.db:/app/database.db
      - ./work:/app/work
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
